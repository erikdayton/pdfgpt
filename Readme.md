# Setup
```shell
# Install dependencies
npm install

# Ensure you have OPEN AI env vars set
export OPENAI_API_KEY=your_key_here

```


# Usage
```shell
# How to run
ts-node index.ts [path_to_pdf] [your question]

# Example
ts-node index.ts ./pdfs/1.pdf "What is the name of the company?"
```